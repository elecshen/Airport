apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: airport-platform
  labels:
    app: admin-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: admin-service
