apiVersion: v1
kind: Service
metadata:
  name: flight-service
  namespace: airport-platform
  labels:
    app: flight-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: flight-service
