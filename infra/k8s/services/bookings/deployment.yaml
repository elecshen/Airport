apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: airport-platform
  labels:
    app: booking-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      initContainers:
        - name: run-migrations
          image: airport-booking-service:latest
          command: ["dotnet", "Airport.Bookings.dll", "--migrate"]
          envFrom:
            - configMapRef:
                name: shared-config
            - secretRef:
                name: db-credentials
          env:
            - name: DB_SCHEMA
              value: "bookings"
      
      containers:
        - name: booking-service
          image: airport-booking-service:latest
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: shared-config
            - secretRef:
                name: db-credentials
          env:
            - name: DB_SCHEMA
              value: "bookings"
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: OTEL_SERVICE_NAME
              value: "booking-service"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
      restartPolicy: Always
